# Documentacion de API (reservado)

FastAPI expone OpenAPI automaticamente en `/docs` y `/openapi.json`.
Esta carpeta queda reservada para exportar ese contrato (ej. para
generar un cliente TypeScript o una coleccion de Postman) cuando los
endpoints dejen de ser stubs.
