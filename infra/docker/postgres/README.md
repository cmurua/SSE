# Scripts de inicializacion de PostgreSQL (reservado)

Carpeta lista para scripts `*.sql` que Postgres ejecuta automaticamente
al crear el volumen por primera vez (extensiones, esquemas). Vacia por
ahora: no hay requerimientos de extensiones confirmados.
