# Nginx (reservado - futuro)

Placeholder para la configuracion de un reverse proxy de produccion
(TLS, servir el build estatico del frontend, proxy_pass al backend).
No forma parte del alcance confirmado (solo se definio despliegue
local/desarrollo con Docker Compose). Ver "evoluciones futuras" en
docs/architecture/overview.md.
