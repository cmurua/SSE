// Puerto directo de ControlBarsPanel.jsx (gauge SVG de barras de control +
// nivel de moderador). No pasa por components/charts/: es una
// visualizacion propia del dominio reactor, no un grafico generico.
export function ControlBarsPanel() {
  return <div>{/* TODO: portar ControlBarsPanel.jsx */}</div>;
}
