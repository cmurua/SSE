// Puerto de Login.jsx del prototipo. TODO: reemplazar el submit simulado
// por una llamada real a authApi.login() y persistir tokens via AuthProvider.
export function LoginPage() {
  return <div className="min-h-screen flex items-center justify-center">{/* TODO: portar Login.jsx */}</div>;
}
