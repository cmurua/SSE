// TODO: test unitario de LoginPage (render + submit invalido muestra error).
import { describe, it, expect } from "vitest";

describe("LoginPage", () => {
  it("placeholder", () => {
    expect(true).toBe(true);
  });
});
