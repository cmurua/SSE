// Puerto de icons.jsx (set de iconos propio, sin dependencia externa).
// Convencion de nombres: prefijo "I" + PascalCase (IHelp, ILogOut, ...).
// TODO: portar el set completo desde el prototipo.
export function IHelp(props: { size?: number; className?: string }) {
  return <svg {...props} />;
}
