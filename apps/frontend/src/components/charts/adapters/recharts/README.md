# Adapter Recharts (reservado - futuro)

Placeholder para cuando se elija Recharts como libreria de graficos.
Debe exportar un componente con la misma firma que `ChartRendererProps`
(ver ../../types.ts) e implementar `activeAdapter.ts` apuntando aca.
