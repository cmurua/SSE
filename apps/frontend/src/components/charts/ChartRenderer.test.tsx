// TODO: test de que ChartRenderer delega en el adapter activo.
import { describe, it, expect } from "vitest";

describe("ChartRenderer", () => {
  it("placeholder", () => {
    expect(true).toBe(true);
  });
});
